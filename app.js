var app = angular.module('userProfiles', []); 

/*var app = This is stating we're declaring an app.

angular.module = This is going to be an Angular module.  Essentially an Angular app

('UserProfiles, []); = This is the name of our app.  Convention is to uppercase the name.  When we declare a module we include the , []. 
I'd still like to get some more clarity on this.


var app = angular.module('MyFirstApp, []);  Declaring an app/module*/

